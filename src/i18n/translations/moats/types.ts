export interface MoatStrings {
  /** Display name of the mechanic. */
  name: string;
  /** What it actually is, in one paragraph. */
  essence: string;
  /** How it gets built — the digging. */
  build: string;
  /** How it gets bypassed — where the water leaks out. */
  bypass: string;
}
